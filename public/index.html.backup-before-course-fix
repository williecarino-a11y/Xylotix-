<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Miimiid</title>  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background: #0f172a;
      color: #f8fafc;
      padding: 15px;
      margin: 0;
    }

    h1 {
      color: #38bdf8;
      font-size: 1.5rem;
      margin-bottom: 20px;
    }

    h2 {
      color: #38bdf8;
      margin-top: 15px;
    }

    h3 {
      color: #f1f5f9;
      margin-top: 0;
    }

    p {
      line-height: 1.7;
      font-size: 0.95rem;
    }

    .card {
      background: #1e293b;
      padding: 15px;
      margin-bottom: 15px;
      border-radius: 8px;
      border: 1px solid #334155;
      overflow-wrap: break-word;
    }

    .btn {
      background: #0284c7;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      width: 100%;
      margin-top: 10px;
    }

    .btn:hover {
      background: #0369a1;
    }

    .btn-sm {
      width: auto;
      padding: 7px 12px;
      font-size: 0.85rem;
      margin-top: 5px;
    }

    .hidden {
      display: none !important;
    }

    #loading {
      color: #38bdf8;
      font-weight: bold;
      margin: 15px 0;
    }

    #error {
      background: #450a0a;
      border: 1px solid #7f1d1d;
      color: #f87171;
      padding: 12px;
      border-radius: 6px;
      margin: 15px 0;
    }

    .success-text {
      color: #4ade80;
      font-weight: bold;
    }

    .error-text {
      color: #f87171;
      font-weight: bold;
    }

    ul {
      padding-left: 20px;
    }

    li {
      margin-bottom: 12px;
    }

    .lesson-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      flex-wrap: wrap;
    }

    .lesson-row span {
      flex: 1;
      min-width: 150px;
    }

    /* ================================
       LESSON CONTENT
       ================================ */

    #lesson-content {
      margin-top: 20px;
    }

    .content-block {
      margin-bottom: 20px;
      line-height: 1.7;
    }

    .content-heading {
      color: #38bdf8;
      margin-bottom: 8px;
    }

    .content-text {
      line-height: 1.8;
      white-space: pre-wrap;
    }

    .content-example {
      background: #172554;
      border-left: 4px solid #38bdf8;
      padding: 14px;
      border-radius: 6px;
    }

    .content-callout {
      background: #422006;
      border-left: 4px solid #f59e0b;
      padding: 14px;
      border-radius: 6px;
    }

    .content-formula {
      background: #020617;
      padding: 14px;
      border-radius: 6px;
      font-family: monospace;
      white-space: pre-wrap;
      overflow-x: auto;
    }

    .content-summary {
      background: #052e16;
      border-left: 4px solid #4ade80;
      padding: 14px;
      border-radius: 6px;
    }

    .content-image {
      display: block;
      max-width: 100%;
      height: auto;
      border-radius: 8px;
      margin-top: 10px;
    }

.content-video {
  display: block;
  width: 100%;
  max-width: 800px;
  height: auto;
  aspect-ratio: 16 / 9;
  object-fit: contain;
  border-radius: 10px;
  margin: 15px auto 0;
  background: #000;
}

.video-wrapper {
  position: relative;
  width: 100%;
  max-width: 800px;
  margin: 0 auto;
}

.video-watermark {
  position: absolute;
  bottom: 12px;
  right: 12px;

  color: rgba(255, 255, 255, 0.75);
  font-size: 14px;
  font-weight: 600;

  padding: 5px 8px;
  border-radius: 4px;

  background: rgba(0, 0, 0, 0.35);

  pointer-events: none;
  user-select: none;

  z-index: 2;
}

    .content-caption {
      color: #94a3b8;
      font-size: 0.85rem;
      margin-top: 6px;
    }

    /* ================================
       QUIZ
       ================================ */

    .quiz-option {
      display: block;
      background: #0f172a;
      border: 1px solid #334155;
      padding: 12px;
      margin: 8px 0;
      border-radius: 6px;
      cursor: pointer;
    }

    .quiz-option:hover {
      border-color: #38bdf8;
    }

    .quiz-option input {
      margin-right: 8px;
    }

/* ================================
   CALCULATION ANSWER INPUT
   ================================ */

.calculation-input {
  width: 100%;
  padding: 14px 16px;
  margin-top: 10px;

  background: #ffffff;
  color: #111827;

  border: 2px solid #cbd5e1;
  border-radius: 8px;

  font-size: 16px;
  font-weight: 500;

  box-sizing: border-box;
  outline: none;
}

.calculation-input:focus {
  background: #ffffff;
  color: #111827;
  border-color: #2563eb;
}

.calculation-input::placeholder {
  color: #64748b;
  opacity: 1;
}
  
  /* ================================
     MIIMIID HEADER + LANGUAGE SELECTOR
     ================================ */

  .miimiid-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 24px;
  }

  .miimiid-brand {
    margin: 0;
    color: #38bdf8;
    font-size: 1.5rem;
    line-height: 1.2;
  }

  .language-selector {
    position: relative;
  }

  .language-toggle {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #1e293b;
    color: #f8fafc;
    border: 1px solid #334155;
    border-radius: 10px;
    padding: 9px 12px;
    cursor: pointer;
    font-size: 0.9rem;
    font-weight: 600;
  }

  .language-toggle:hover,
  .language-toggle:focus-visible {
    border-color: #38bdf8;
    background: #263449;
    outline: none;
  }

  .language-toggle-icon {
    font-size: 1rem;
  }

  .language-chevron {
    font-size: 0.75rem;
    transition: transform 0.2s;
  }

  .language-selector.open .language-chevron {
    transform: rotate(180deg);
  }

  .language-menu {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    width: 230px;
    max-height: 360px;
    overflow-y: auto;
    background: #1e293b;
    border: 1px solid #334155;
    border-radius: 12px;
    padding: 8px;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.35);
    z-index: 1000;
  }

  .language-menu.hidden {
    display: none;
  }

  .language-option {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    background: transparent;
    color: #f8fafc;
    border: none;
    border-radius: 8px;
    padding: 11px 12px;
    cursor: pointer;
    text-align: left;
    font-size: 0.9rem;
  }

  .language-option:hover,
  .language-option:focus-visible {
    background: #334155;
    outline: none;
  }

  .language-option.active {
    background: #0c4a6e;
    color: #7dd3fc;
  }

  .language-option-name {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .language-option-native {
    color: #94a3b8;
    font-size: 0.78rem;
  }

  .language-option.active .language-option-native {
    color: #bae6fd;
  }

  .language-check {
    font-weight: bold;
    color: #4ade80;
  }

  @media (max-width: 520px) {
    .miimiid-header {
      align-items: flex-start;
    }

    .language-toggle {
      padding: 8px 10px;
    }

    .language-toggle-label {
      display: none;
    }

    .language-menu {
      width: min(230px, calc(100vw - 30px));
    }
  }

</style></head><body>  <header class="miimiid-header">
    <h1 class="miimiid-brand">Miimiid</h1>

    <div class="language-selector" id="language-selector">
      <button
        type="button"
        class="language-toggle"
        id="language-toggle"
        aria-haspopup="true"
        aria-expanded="false"
        aria-label="Select language"
      >
        <span class="language-toggle-icon">🌍</span>
        <span class="language-toggle-label" id="selected-language-label">English</span>
        <span class="language-chevron">▼</span>
      </button>

      <div
        class="language-menu hidden"
        id="language-menu"
        role="menu"
        aria-label="Language options"
      >
        <button type="button" class="language-option active" data-language="en" role="menuitem">
          <span class="language-option-name">
            <span>English</span>
            <span class="language-option-native">English</span>
          </span>
          <span class="language-check">✓</span>
        </button>

        <button type="button" class="language-option" data-language="es" role="menuitem">
          <span class="language-option-name">
            <span>Spanish</span>
            <span class="language-option-native">Español</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="fr" role="menuitem">
          <span class="language-option-name">
            <span>French</span>
            <span class="language-option-native">Français</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="de" role="menuitem">
          <span class="language-option-name">
            <span>German</span>
            <span class="language-option-native">Deutsch</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="pt" role="menuitem">
          <span class="language-option-name">
            <span>Portuguese</span>
            <span class="language-option-native">Português</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="zh" role="menuitem">
          <span class="language-option-name">
            <span>Chinese</span>
            <span class="language-option-native">中文</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="ja" role="menuitem">
          <span class="language-option-name">
            <span>Japanese</span>
            <span class="language-option-native">日本語</span>
          </span>
        </button>

        <button type="button" class="language-option" data-language="ko" role="menuitem">
          <span class="language-option-name">
            <span>Korean</span>
            <span class="language-option-native">한국어</span>
          </span>
        </button>
      </div>
    </div>
  </header>  <div id="loading" class="hidden">
    Loading...
  </div>  <div id="error" class="hidden"></div>  <!-- ================================
       COURSE LIST
       ================================ -->  <div id="course-list-view"><p>
  Select a course to explore modules and lessons:
</p>

<div id="courses-container"></div>

  </div>  <!-- ================================
       COURSE DETAILS
       ================================ -->  <div id="course-detail-view" class="hidden"><button
  class="btn btn-sm"
  onclick="goBackToCourses()"
>
  ← Back to Courses
</button>

<h2 id="detail-course-title"></h2>

<p id="detail-course-desc"></p>

<div id="modules-container"></div>

  </div>  <!-- ================================
       LESSON VIEW
       ================================ -->  <div id="lesson-view" class="card hidden"><button
  class="btn btn-sm"
  onclick="backToModules()"
>
  ← Back to Course Modules
</button>

<h2 id="lesson-title"></h2>

<div id="lesson-content"></div>

<!-- QUIZ -->

<div id="quiz-section" class="hidden">

  <h3>Quick Check</h3>

  <p id="quiz-question"></p>

  <div id="quiz-options"></div>

  <button
    id="quiz-submit-btn"
    class="btn"
    onclick="submitQuiz()"
  >
    Submit Answer
  </button>

  <p id="quiz-result"></p>

</div>

<!-- COMPLETE LESSON -->

<button
  id="complete-btn"
  class="btn"
  onclick="completeLesson()"
>
  Complete Lesson
</button>

  </div>  <script>

    /* =========================================
       APPLICATION STATE
       ========================================= */

let currentQuizzes = [];
let selectedQuizAnswers = [];
let quizSubmitted = false;
let currentCourseId = null;
let currentLessonId = null;
let currentQuiz = null;
let courseDataCache = null;

    /*
     * Temporary user ID.
     * This should eventually come from authentication.
     */
    const dummyUserId = "648fc928d566fb4c30676e11";


    /* =========================================
       FETCH ALL COURSES
       ========================================= */

async function fetchCourses() {
  showLoading(true);
  hideError();

  try {
    const response = await fetch("/api/learn/courses");
    const data = await response.json();

    const container =
      document.getElementById("courses-container");

    if (
      response.ok &&
      data.status === "success" &&
      Array.isArray(data.data) &&
      data.data.length > 0
    ) {
      container.innerHTML = data.data
        .map(
          course => `
            <div class="card">

              <h3>
                ${escapeHtml(
                  course.title || "Untitled Course"
                )}
              </h3>

              <p>
                ${escapeHtml(
                  course.description ||
                  "No description available."
                )}
              </p>

              <button
                class="btn"
                onclick="openCourse('${course._id}')"
              >
                Open Course
              </button>

            </div>
          `
        )
        .join("");
    } else {
      container.innerHTML = `
        <div class="card">
          <p>
            No courses available yet.
          </p>
        </div>
      `;
    }

  } catch (error) {
    console.error("fetchCourses error:", error);

    showError(
      "Unable to load courses."
    );

  } finally {
    showLoading(false);
  }
}

    /* =========================================
       OPEN COURSE
       ========================================= */

async function openCourse(courseId) {
  if (!courseId) {
    showError("Invalid course ID.");
    return;
  }

  currentCourseId = courseId;

  showLoading(true);
  hideError();

  try {
    const response = await fetch(
      `/api/learn/courses/${courseId}`
    );

    const data = await response.json();

    if (
      response.ok &&
      data.status === "success" &&
      data.data
    ) {
      courseDataCache = data.data;

      document
        .getElementById("course-list-view")
        .classList.add("hidden");

      document
        .getElementById("lesson-view")
        .classList.add("hidden");

      document
        .getElementById("course-detail-view")
        .classList.remove("hidden");

      document
        .getElementById("detail-course-title")
        .innerText =
          courseDataCache.title ||
          "Untitled Course";

      document
        .getElementById("detail-course-desc")
        .innerText =
          courseDataCache.description ||
          "No description available.";

      renderModules(
        courseDataCache.modules
      );

    } else {
      console.error(
        "Course loading failed:",
        data
      );

      showError(
        data.message ||
        "Unable to load course."
      );
    }

  } catch (error) {
    console.error(
      "openCourse error:",
      error
    );

    showError(
      "Unable to connect to the server."
    );

  } finally {
    showLoading(false);
  }
}

    /* =========================================
       RENDER MODULES
       ========================================= */

    function renderModules(modules) {

      const container =
        document.getElementById(
          "modules-container"
        );

      if (
        !Array.isArray(modules) ||
        modules.length === 0
      ) {

        container.innerHTML = `
          <div class="card">
            <p>
              No modules are available yet.
            </p>
          </div>
        `;

        return;
      }

      container.innerHTML =
        modules.map(
          (module, moduleIndex) => {

            const lessons =
              Array.isArray(
                module.lessons
              )
                ? module.lessons
                : [];

            return `

              <div class="card">

              <h3>
              ${escapeHtml(
              module.title ||
             `Module ${moduleIndex + 1}`
             )}
          </h3> 

                ${
                  lessons.length === 0

                    ? `
                      <p>
                        No lessons available
                        in this module yet.
                      </p>
                    `

                    : `
                      <ul>

                        ${
                          lessons.map(
                            lesson => `

                              <li>

                                <div
                                  class="lesson-row"
                                >

                                  <span>
                                    ${escapeHtml(
                                      lesson.title ||
                                      "Untitled Lesson"
                                    )}
                                  </span>

                                  <button
                                    class="btn btn-sm"
                                    onclick="openLesson('${lesson._id}')"
                                  >
                                    Start Lesson
                                  </button>

                                </div>

                              </li>

                            `
                          ).join("")
                        }

                      </ul>
                    `
                }

              </div>

            `;

          }
        ).join("");

    }


    /* =========================================
       OPEN LESSON
       ========================================= */

    function openLesson(lessonId) {

      currentLessonId = lessonId;

      if (
        !courseDataCache ||
        !Array.isArray(
          courseDataCache.modules
        )
      ) {

        showError(
          "Course data is unavailable."
        );

        return;
      }

      let targetLesson = null;

      /*
       * Find the lesson inside:
       *
       * Course
       *   -> Modules
       *       -> Lessons
       */

      for (
        const module
        of courseDataCache.modules
      ) {

        if (
          !Array.isArray(
            module.lessons
          )
        ) {
          continue;
        }

        const found =
          module.lessons.find(
            lesson =>
              String(
                lesson._id
              ) === String(
                lessonId
              )
          );

        if (found) {

          targetLesson = found;

          break;

        }

      }

      if (!targetLesson) {

        alert(
          "Lesson not found."
        );

        return;
      }

      document
        .getElementById(
          "course-detail-view"
        )
        .classList.add(
          "hidden"
        );

      document
        .getElementById(
          "lesson-view"
        )
        .classList.remove(
          "hidden"
        );

      document
        .getElementById(
          "lesson-title"
        )
        .innerText =
          targetLesson.title ||
          "Untitled Lesson";

      /*
       * IMPORTANT:
       *
       * Your Lesson model uses
       * contentBlocks.
       *
       * It does NOT use:
       *
       * lesson.content
       */

      renderLessonContent(
        targetLesson.contentBlocks
      );

      renderQuiz(
        targetLesson
      );

    }


    /* =========================================
       RENDER LESSON CONTENT
       ========================================= */

    function renderLessonContent(
      contentBlocks
    ) {

      const container =
        document.getElementById(
          "lesson-content"
        );

      if (
        !Array.isArray(
          contentBlocks
        ) ||
        contentBlocks.length === 0
      ) {

        container.innerHTML = `
          <div class="card">
            <p>
              No lesson content available.
            </p>
          </div>
        `;

        return;
      }

      /*
       * Sort blocks by their database order.
       */

      const blocks =
        [...contentBlocks].sort(
          (a, b) =>
            Number(a.order || 0) -
            Number(b.order || 0)
        );

      container.innerHTML =
        blocks
          .map(
            block =>
              renderContentBlock(
                block
              )
          )
          .join("");

    }


    /* =========================================
       RENDER ONE CONTENT BLOCK
       ========================================= */

    function renderContentBlock(block) {

      if (!block) {
        return "";
      }

      const type =
        block.type || "text";

      const data =
        block.data;

      const text =
        getBlockText(data);

      switch (type) {

        case "heading":

          return `
            <div
              class="content-block"
            >

              <h3
                class="content-heading"
              >
                ${escapeHtml(text)}
              </h3>

            </div>
          `;


        case "text":

          return `
            <div
              class="content-block content-text"
            >
              ${escapeHtml(text)}
            </div>
          `;


        case "example":

          return `
            <div
              class="content-block content-example"
            >

              <strong>
                Example
              </strong>

              <p>
                ${escapeHtml(text)}
              </p>

            </div>
          `;


        case "callout":

          return `
            <div
              class="content-block content-callout"
            >

              <strong>
                Important
              </strong>

              <p>
                ${escapeHtml(text)}
              </p>

            </div>
          `;


        case "formula":

          return `
            <div
              class="content-block content-formula"
            >
              ${escapeHtml(text)}
            </div>
          `;


        case "summary":

          return `
            <div
              class="content-block content-summary"
            >

              <strong>
                Summary
              </strong>

              <p>
                ${escapeHtml(text)}
              </p>

            </div>
          `;


        case "image":

          return renderImageBlock(
            data
          );


        case "video":

          return renderVideoBlock(
            data
          );


        case "interactive":

          return `
            <div
              class="content-block card"
            >
              <p>
                ${escapeHtml(text)}
              </p>
            </div>
          `;


        default:

          return `
            <div
              class="content-block"
            >
              <p>
                ${escapeHtml(text)}
              </p>
            </div>
          `;

      }

    }


    /* =========================================
       EXTRACT TEXT FROM CONTENT DATA
       ========================================= */

function getBlockText(data) {

  if (
    data === null ||
    data === undefined
  ) {
    return "";
  }

  if (
    typeof data === "string"
  ) {
    return data;
  }

  if (
    typeof data === "number" ||
    typeof data === "boolean"
  ) {
    return String(data);
  }

  if (
    typeof data === "object"
  ) {

    return (
      data.text ??
      data.content ??
      data.value ??
      data.description ??
      data.body ??
      data.formula ??
      data.equation ??
      data.expression ??
      data.title ??
      ""
    );

  }

  return String(data);
}

    /* =========================================
       IMAGE BLOCK
       ========================================= */

    function renderImageBlock(data) {

      const source =
        getMediaSource(data);

      if (!source) {

        return `
          <div class="content-block">
            <p>
              Image unavailable.
            </p>
          </div>
        `;

      }

      const alt =
        data &&
        typeof data === "object"
          ? (
              data.alt ||
              data.caption ||
              "Lesson image"
            )
          : "Lesson image";

const caption =
        data &&
        typeof data === "object"
          ? data.caption || ""
          : "";

      return `

        <div class="content-block">

          <img
            class="content-image"
            src="${escapeAttribute(source)}"
            alt="${escapeAttribute(alt)}"
            loading="lazy"
          >

          ${
            caption
              ? `
                <div class="content-caption">
                  ${escapeHtml(caption)}
                </div>
              `
              : ""
          }

        </div>

      `;

    }


    /* =========================================
   VIDEO BLOCK
   ========================================= */

function renderVideoBlock(data) {

  const source =
    getMediaSource(data);

  if (!source) {

    return `
      <div class="content-block">
        <p>
          Video unavailable.
        </p>
      </div>
    `;

  }

  return `

    <div class="content-block">

      <div class="video-wrapper">

        <video
          class="content-video"
          controls
          preload="metadata"
          playsinline
        >

          <source
            src="${escapeAttribute(source)}"
          >

          Your browser does not support
          video playback.

        </video>

        <div class="video-watermark">
          Miimiid AI Tutor
        </div>

      </div>

    </div>

  `;

}


    /* =========================================
       GET MEDIA URL
       ========================================= */

    function getMediaSource(data) {

      if (
        typeof data === "string"
      ) {
        return data;
      }

      if (
        !data ||
        typeof data !== "object"
      ) {
        return "";
      }

      return (
        data.url ||
        data.src ||
        data.source ||
        data.href ||
        ""
      );

    }


    /* =========================================
       QUIZ
       ========================================= */

function renderQuiz(lesson) {

  const section =
    document.getElementById("quiz-section");

  const question =
    document.getElementById("quiz-question");

  const options =
    document.getElementById("quiz-options");

  const result =
    document.getElementById("quiz-result");

  result.innerHTML = "";

  currentQuizzes =
    Array.isArray(lesson.quizzes)
      ? lesson.quizzes
      : [];

  selectedQuizAnswers =
    new Array(currentQuizzes.length);

  quizSubmitted = false;

  if (currentQuizzes.length === 0) {

    section.classList.add("hidden");
    options.innerHTML = "";

    return;
  }

  section.classList.remove("hidden");

  /*
   * Render every question according
   * to its question type.
   */
  options.innerHTML =
    currentQuizzes
      .map((quiz, quizIndex) => {

        const questionType =
          quiz.questionType || "multiple-choice";

        /*
         * -----------------------------------------
         * MULTIPLE-CHOICE / TRUE-FALSE
         * -----------------------------------------
         */
        if (
          questionType === "multiple-choice" ||
          questionType === "true-false"
        ) {

          const quizOptions =
            Array.isArray(quiz.options)
              ? quiz.options
              : [];

          return `
            <div
              class="card"
              style="margin-top: 15px;"
            >

              <p>
                <strong>
                  ${quizIndex + 1}.
                  ${escapeHtml(
                    quiz.question ||
                    "Question unavailable."
                  )}
                </strong>
              </p>

              ${
                quizOptions.length > 0

                  ? quizOptions
                      .map(
                        (option, optionIndex) => `
                          <label
                            class="quiz-option"
                          >

                            <input
                              type="radio"
                              name="quiz-${quizIndex}"
                              value="${optionIndex}"
                              onchange="
                                selectedQuizAnswers[${quizIndex}]
                                = ${optionIndex};
                              "
                            >

                            ${escapeHtml(option)}

                          </label>
                        `
                      )
                      .join("")

                  : `
                    <p class="error-text">
                      No answer options available
                      for this question.
                    </p>
                  `
              }

            </div>
          `;
        }

        /*
         * -----------------------------------------
         * CALCULATION QUESTION
         * -----------------------------------------
         */
        if (questionType === "calculate") {

          return `
            <div
              class="card"
              style="margin-top: 15px;"
            >

              <p>
                <strong>
                  ${quizIndex + 1}.
                  ${escapeHtml(
                    quiz.question ||
                    "Calculation question unavailable."
                  )}
                </strong>
              </p>

              <label>
                <strong>
                  Your answer:
                </strong>
              </label>

               <input
               type="number"
               class="quiz-option calculation-input"
               id="calculation-answer-${quizIndex}"
                placeholder="Enter your answer"
                step="any"
                oninput="
                  selectedQuizAnswers[${quizIndex}]
                  = this.value;
                "
              >

              ${
                quiz.hint
                  ? `
                    <p>
                      <small>
                        Hint:
                        ${escapeHtml(quiz.hint)}
                      </small>
                    </p>
                  `
                  : ""
              }

            </div>
          `;
        }

        /*
         * -----------------------------------------
         * UNKNOWN QUESTION TYPE
         * -----------------------------------------
         */
        return `
          <div
            class="card"
            style="margin-top: 15px;"
          >

            <p>
              <strong>
                ${quizIndex + 1}.
                ${escapeHtml(
                  quiz.question ||
                  "Question unavailable."
                )}
              </strong>
            </p>

            <p class="error-text">
              Unsupported question type:
              ${escapeHtml(questionType)}
            </p>

          </div>
        `;

      })
      .join("");

  question.innerText =
    "Answer the questions below before completing the lesson.";
}

    /* =========================================
       SUBMIT QUIZ
       ========================================= */

async function submitQuiz() {

  if (
    !currentLessonId ||
    currentQuizzes.length === 0
  ) {

    alert("No quiz is available.");

    return;
  }

  /*
   * Make sure every question has an answer.
   */
  const unanswered =
    currentQuizzes.findIndex(
      (quiz, index) => {

        const answer =
          selectedQuizAnswers[index];

        if (
          answer === undefined ||
          answer === null ||
          answer === ""
        ) {
          return true;
        }

        return false;
      }
    );

  if (unanswered !== -1) {

    alert(
      `Please answer question ${
        unanswered + 1
      } before submitting.`
    );

    return;
  }

  /*
   * Convert calculation answers
   * from strings to numbers.
   *
   * Multiple-choice and true-false
   * answers remain option indexes.
   */
  const answersForSubmission =
    currentQuizzes.map(
      (quiz, index) => {

        if (
          quiz.questionType === "calculate"
        ) {

          return Number(
            selectedQuizAnswers[index]
          );

        }

        return selectedQuizAnswers[index];

      }
    );

  const button =
    document.getElementById(
      "quiz-submit-btn"
    );

  const result =
    document.getElementById(
      "quiz-result"
    );

  button.disabled = true;
  button.innerText = "Checking...";

  try {

    const response =
      await fetch(
        "/api/learn/quiz/submit",
        {
          method: "POST",

          headers: {
            "Content-Type":
              "application/json"
          },

          body: JSON.stringify({

            lessonId:
              currentLessonId,

            submittedAnswers:
              answersForSubmission

          })
        }
      );

    const data =
      await response.json();

    if (
      response.ok &&
      data.status === "success"
    ) {

      const quizResult =
        data.data;

      if (quizResult.passed) {

        result.innerHTML = `
          <span class="success-text">
            Quiz complete!
            Score: ${quizResult.score}%.
            You got
            ${quizResult.correctAnswers}
            out of
            ${quizResult.totalQuestions}
            correct.
            You can now complete the lesson.
          </span>
        `;

        quizSubmitted = true;

      } else {

        result.innerHTML = `
          <span class="error-text">
            Score: ${quizResult.score}%.
            You got
            ${quizResult.correctAnswers}
            out of
            ${quizResult.totalQuestions}
            correct.
            Review the lesson and try again.
          </span>
        `;

        quizSubmitted = false;
      }

    } else {

      result.innerHTML = `
        <span class="error-text">
          ${
            data.message ||
            "Unable to validate quiz."
          }
        </span>
      `;

    }

  } catch (error) {

    console.error(
      "Quiz submission error:",
      error
    );

    result.innerHTML = `
      <span class="error-text">
        Unable to connect to the server.
      </span>
    `;

  } finally {

    button.disabled = false;
    button.innerText = "Submit Answer";

  }
}

    /* =========================================
       COMPLETE LESSON
       ========================================= */

    async function completeLesson() {

  if (!currentLessonId) {

    alert(
      "No lesson is currently selected."
    );

    return;
  }

  /*
   * If the lesson has quizzes,
   * require the quiz to be submitted first.
   */
  if (
    currentQuizzes.length > 0 &&
    !quizSubmitted
  ) {

    alert(
      "Please complete and submit the quiz before completing this lesson."
    );

    return;
  }

  const button =
    document.getElementById(
      "complete-btn"
    );

  button.disabled = true;

  button.innerText =
    "Saving...";

  try {

    const response =
      await fetch(
        `/api/learn/lessons/${currentLessonId}/complete`,
        {
          method: "POST",

          headers: {
            "Content-Type":
              "application/json"
          },

          body: JSON.stringify({

            userId:
              dummyUserId,

            submittedAnswers:
              selectedQuizAnswers

          })
        }
      );

    const data =
      await response.json();

    if (
      response.ok &&
      data.status === "success"
    ) {

      alert(
        "Lesson completed successfully!"
      );

      /*
       * Reload the course so the
       * frontend has fresh data.
       */
      await openCourse(
        currentCourseId
      );

    } else {

      alert(
        data.message ||
        "Failed to save lesson progress."
      );

    }

  } catch (error) {

    console.error(
      "Complete lesson error:",
      error
    );

    alert(
      "Failed to save progress."
    );

  } finally {

    button.disabled = false;

    button.innerText =
      "Complete Lesson";
  }
}


    /* =========================================
       BACK TO MODULES
       ========================================= */

    function backToModules() {

      document
        .getElementById(
          "lesson-view"
        )
        .classList.add(
          "hidden"
        );

      document
        .getElementById(
          "course-detail-view"
        )
        .classList.remove(
          "hidden"
        );

    }


    /* =========================================
       BACK TO COURSES
       ========================================= */

    function goBackToCourses() {

      currentCourseId = null;
      currentLessonId = null;
      courseDataCache = null;

      document
        .getElementById(
          "course-detail-view"
        )
        .classList.add(
          "hidden"
        );

      document
        .getElementById(
          "lesson-view"
        )
        .classList.add(
          "hidden"
        );

      document
        .getElementById(
          "course-list-view"
        )
        .classList.remove(
          "hidden"
        );

      fetchCourses();

    }


    /* =========================================
       LOADING
       ========================================= */

    function showLoading(isLoading) {

      const loading =
        document.getElementById(
          "loading"
        );

      if (isLoading) {

        loading.classList.remove(
          "hidden"
        );

      } else {

        loading.classList.add(
          "hidden"
        );

      }

    }


    /* =========================================
       ERROR
       ========================================= */

    function showError(message) {

      const error =
        document.getElementById(
          "error"
        );

      error.innerText =
        message ||
        "Something went wrong.";

      error.classList.remove(
        "hidden"
      );

    }


    function hideError() {

      document
        .getElementById(
          "error"
        )
        .classList.add(
          "hidden"
        );

    }


    /* =========================================
       HTML ESCAPING
       ========================================= */

    function escapeHtml(value) {

      const div =
        document.createElement(
          "div"
        );

      div.textContent =
        value === null ||
        value === undefined
          ? ""
          : String(value);

      return div.innerHTML;

    }


    function escapeAttribute(value) {

      return escapeHtml(
        value
      )
      .replace(
        /"/g,
        "&quot;"
      )
      .replace(
        /'/g,
        "&#39;"
      );

    }


    /* =========================================
       START APPLICATION
       ========================================= */

    fetchCourses();

  </script>


<script>
  /* ================================
     MIIMIID LANGUAGE SELECTOR
     ================================ */

  const MIIMIID_LANGUAGE_KEY = "miimiid-language";

  const MIIMIID_LANGUAGES = {
    en: {
      name: "English",
      nativeName: "English"
    },
    es: {
      name: "Spanish",
      nativeName: "Español"
    },
    fr: {
      name: "French",
      nativeName: "Français"
    },
    de: {
      name: "German",
      nativeName: "Deutsch"
    },
    pt: {
      name: "Portuguese",
      nativeName: "Português"
    },
    zh: {
      name: "Chinese",
      nativeName: "中文"
    },
    ja: {
      name: "Japanese",
      nativeName: "日本語"
    },
    ko: {
      name: "Korean",
      nativeName: "한국어"
    }
  };

  function getSavedLanguage() {
    try {
      const saved = localStorage.getItem(MIIMIID_LANGUAGE_KEY);

      if (saved && MIIMIID_LANGUAGES[saved]) {
        return saved;
      }
    } catch (error) {
      console.warn("Miimiid could not read saved language preference.");
    }

    return "en";
  }

  function saveLanguage(language) {
    try {
      localStorage.setItem(MIIMIID_LANGUAGE_KEY, language);
    } catch (error) {
      console.warn("Miimiid could not save language preference.");
    }
  }

  function updateLanguageUI(language) {
    const languageData = MIIMIID_LANGUAGES[language];

    if (!languageData) {
      return;
    }

    const label = document.getElementById("selected-language-label");

    if (label) {
      label.textContent = languageData.name;
    }

    document.querySelectorAll(".language-option").forEach(option => {
      const isActive = option.dataset.language === language;

      option.classList.toggle("active", isActive);

      const existingCheck = option.querySelector(".language-check");

      if (isActive && !existingCheck) {
        const check = document.createElement("span");
        check.className = "language-check";
        check.textContent = "✓";
        option.appendChild(check);
      } else if (!isActive && existingCheck) {
        existingCheck.remove();
      }
    });

    document.documentElement.lang = language;
  }

  function closeLanguageMenu() {
    const selector = document.getElementById("language-selector");
    const toggle = document.getElementById("language-toggle");
    const menu = document.getElementById("language-menu");

    if (!selector || !toggle || !menu) {
      return;
    }

    selector.classList.remove("open");
    menu.classList.add("hidden");
    toggle.setAttribute("aria-expanded", "false");
  }

  function openLanguageMenu() {
    const selector = document.getElementById("language-selector");
    const toggle = document.getElementById("language-toggle");
    const menu = document.getElementById("language-menu");

    if (!selector || !toggle || !menu) {
      return;
    }

    selector.classList.add("open");
    menu.classList.remove("hidden");
    toggle.setAttribute("aria-expanded", "true");
  }

  function toggleLanguageMenu() {
    const selector = document.getElementById("language-selector");

    if (!selector) {
      return;
    }

    if (selector.classList.contains("open")) {
      closeLanguageMenu();
    } else {
      openLanguageMenu();
    }
  }

  function selectLanguage(language) {
    if (!MIIMIID_LANGUAGES[language]) {
      return;
    }

    saveLanguage(language);
    updateLanguageUI(language);
    closeLanguageMenu();

    /*
      Translation/localization will connect here next.
      For now, the selected language is persisted and
      exposed through document.documentElement.lang.
    */

    document.dispatchEvent(
      new CustomEvent("miimiidLanguageChanged", {
        detail: {
          language,
          languageData: MIIMIID_LANGUAGES[language]
        }
      })
    );
  }

  function initializeLanguageSelector() {
    const toggle = document.getElementById("language-toggle");

    if (!toggle) {
      return;
    }

    const savedLanguage = getSavedLanguage();

    updateLanguageUI(savedLanguage);

    toggle.addEventListener("click", toggleLanguageMenu);

    document.querySelectorAll(".language-option").forEach(option => {
      option.addEventListener("click", () => {
        selectLanguage(option.dataset.language);
      });
    });

    document.addEventListener("click", event => {
      const selector = document.getElementById("language-selector");

      if (selector && !selector.contains(event.target)) {
        closeLanguageMenu();
      }
    });

    toggle.addEventListener("keydown", event => {
      if (event.key === "Escape") {
        closeLanguageMenu();
        toggle.focus();
      }
    });

    document.addEventListener("keydown", event => {
      if (event.key === "Escape") {
        closeLanguageMenu();
      }
    });
  }

  if (document.readyState === "loading") {
    document.addEventListener(
      "DOMContentLoaded",
      initializeLanguageSelector
    );
  } else {
    initializeLanguageSelector();
  }
</script>

</body>
</html>
